# Fabric Pipeline SQL

These scripts are for external Microsoft Fabric pipeline database work. They are intentionally not part of EF Core migrations or the application SQL project because they target pipeline-managed external data surfaces.

